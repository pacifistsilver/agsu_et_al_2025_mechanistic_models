# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
sdist/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter
.ipynb_checkpoints

# Environments
.env
.venv
env/
venv/
ENV/
.conda/
.python-version

# IDEs / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# ---------------------------------------------------------------------------
# Project outputs and data
#
# Extension bans are scoped to directories rather than global. The previous
# global `*.png` / `*.csv` / `*.json` rules meant the paper's own figures could
# not be committed, and silently swallowed config files.
# ---------------------------------------------------------------------------

# Raw data is downloaded, never committed (see scripts/download_data.py)
/data/raw/
/data/*.npy

# Inference traces and simulation outputs are regenerated
/results/
*.nc

# Snakemake and scratch
/.snakemake/
/.archive/
/output/
*.log
*.txt